{{#link-to 'accounts.account.transactions' model.id}}{{model.name}}{{/link-to}}

<ul>
  {{#each model.children as |account|}}
    <li>
      {{render 'account-tree' account}}
    </li>
  {{/each}}
</ul>